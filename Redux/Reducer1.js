
import  {actions} from './ActionTypes'

const initialState = {
    userData: [ ]
};

const Reducer1 = (state = initialState, action) => {
    // switch (action.type) {
        
    //     default:
        
    // }
    return state;
}

export default Reducer1
